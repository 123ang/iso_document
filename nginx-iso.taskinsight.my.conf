# Nginx Configuration for iso.taskinsight.my
# Place this file at: /etc/nginx/sites-available/iso.taskinsight.my

# Backend API - Proxy to Node.js
upstream backend {
    server localhost:3000;
    keepalive 64;
}

# Frontend - Next.js
server {
    listen 80;
    server_name iso.taskinsight.my www.iso.taskinsight.my;

    # Frontend
    location / {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Backend API
    location /api {
        proxy_pass http://backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Increase timeouts for file uploads
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
        client_max_body_size 1G;
    }

    # File upload size limit
    client_max_body_size 1G;
}

# After SSL setup, Certbot will add SSL configuration here
# The server block above will be updated to include:
# listen 443 ssl;
# ssl_certificate /etc/letsencrypt/live/iso.taskinsight.my/fullchain.pem;
# ssl_certificate_key /etc/letsencrypt/live/iso.taskinsight.my/privkey.pem;
# And a redirect from HTTP to HTTPS
